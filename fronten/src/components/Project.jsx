import React from 'react'
import {Container, Row, Column} from 'react-bootstrap/'

export default ({ projects }) => {
  return (
      projects && projects.map((text, index) =>
        <Container fluid>
        
        </Container>
      )
  )
}
